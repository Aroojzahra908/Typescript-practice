type student={
    name:string,
    email:string,
}
function sortedstudent<T>(arr:T[]):T[] {
return arr
}
// ????

// interface:
interface student1{
    rollno:number,
    uni:string,

}
type A= number | null   //define type 
// type is used for scaler type >>> contain = sign 

// interface is not used for scaler type>>>>>>interface only for objects>>> and type
//  for both interface and objects>>>> interface has no = sign 



var anyvar:number=234
let newObj:student1={
    rollno:234,
    uni:"ntu",
} 

